# rendrui


to start tailwind for the sample project navigate to the root of the project and run
```bash
npx @tailwindcss/cli -i ./src/input.css -o ./src/output.css --watch
```

then to run the project

```bash
dotnet watch run --project src/Rendr.Sampl
```